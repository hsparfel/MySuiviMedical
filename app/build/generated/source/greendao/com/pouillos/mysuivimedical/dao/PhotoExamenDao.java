package com.pouillos.mysuivimedical.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.mysuivimedical.entities.Examen;
import com.pouillos.mysuivimedical.entities.RdvExamen;

import com.pouillos.mysuivimedical.entities.PhotoExamen;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PHOTO_EXAMEN".
*/
public class PhotoExamenDao extends AbstractDao<PhotoExamen, Long> {

    public static final String TABLENAME = "PHOTO_EXAMEN";

    /**
     * Properties of entity PhotoExamen.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Path = new Property(1, String.class, "path", false, "PATH");
        public final static Property ExamenId = new Property(2, long.class, "examenId", false, "EXAMEN_ID");
        public final static Property RdvExamenId = new Property(3, long.class, "rdvExamenId", false, "RDV_EXAMEN_ID");
        public final static Property Date = new Property(4, java.util.Date.class, "date", false, "DATE");
        public final static Property DateString = new Property(5, String.class, "dateString", false, "DATE_STRING");
    }

    private DaoSession daoSession;


    public PhotoExamenDao(DaoConfig config) {
        super(config);
    }
    
    public PhotoExamenDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PHOTO_EXAMEN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"PATH\" TEXT," + // 1: path
                "\"EXAMEN_ID\" INTEGER NOT NULL ," + // 2: examenId
                "\"RDV_EXAMEN_ID\" INTEGER NOT NULL ," + // 3: rdvExamenId
                "\"DATE\" INTEGER," + // 4: date
                "\"DATE_STRING\" TEXT);"); // 5: dateString
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PHOTO_EXAMEN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, PhotoExamen entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String path = entity.getPath();
        if (path != null) {
            stmt.bindString(2, path);
        }
        stmt.bindLong(3, entity.getExamenId());
        stmt.bindLong(4, entity.getRdvExamenId());
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(5, date.getTime());
        }
 
        String dateString = entity.getDateString();
        if (dateString != null) {
            stmt.bindString(6, dateString);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, PhotoExamen entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String path = entity.getPath();
        if (path != null) {
            stmt.bindString(2, path);
        }
        stmt.bindLong(3, entity.getExamenId());
        stmt.bindLong(4, entity.getRdvExamenId());
 
        java.util.Date date = entity.getDate();
        if (date != null) {
            stmt.bindLong(5, date.getTime());
        }
 
        String dateString = entity.getDateString();
        if (dateString != null) {
            stmt.bindString(6, dateString);
        }
    }

    @Override
    protected final void attachEntity(PhotoExamen entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public PhotoExamen readEntity(Cursor cursor, int offset) {
        PhotoExamen entity = new PhotoExamen( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // path
            cursor.getLong(offset + 2), // examenId
            cursor.getLong(offset + 3), // rdvExamenId
            cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)), // date
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // dateString
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, PhotoExamen entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPath(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setExamenId(cursor.getLong(offset + 2));
        entity.setRdvExamenId(cursor.getLong(offset + 3));
        entity.setDate(cursor.isNull(offset + 4) ? null : new java.util.Date(cursor.getLong(offset + 4)));
        entity.setDateString(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(PhotoExamen entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(PhotoExamen entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(PhotoExamen entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getExamenDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getRdvExamenDao().getAllColumns());
            builder.append(" FROM PHOTO_EXAMEN T");
            builder.append(" LEFT JOIN EXAMEN T0 ON T.\"EXAMEN_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN RDV_EXAMEN T1 ON T.\"RDV_EXAMEN_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected PhotoExamen loadCurrentDeep(Cursor cursor, boolean lock) {
        PhotoExamen entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Examen examen = loadCurrentOther(daoSession.getExamenDao(), cursor, offset);
         if(examen != null) {
            entity.setExamen(examen);
        }
        offset += daoSession.getExamenDao().getAllColumns().length;

        RdvExamen rdvExamen = loadCurrentOther(daoSession.getRdvExamenDao(), cursor, offset);
         if(rdvExamen != null) {
            entity.setRdvExamen(rdvExamen);
        }

        return entity;    
    }

    public PhotoExamen loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<PhotoExamen> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<PhotoExamen> list = new ArrayList<PhotoExamen>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<PhotoExamen> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<PhotoExamen> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
