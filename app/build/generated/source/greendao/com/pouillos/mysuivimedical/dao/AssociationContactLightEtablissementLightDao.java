package com.pouillos.mysuivimedical.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.mysuivimedical.entities.ContactLight;
import com.pouillos.mysuivimedical.entities.EtablissementLight;

import com.pouillos.mysuivimedical.entities.AssociationContactLightEtablissementLight;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ASSOCIATION_CONTACT_LIGHT_ETABLISSEMENT_LIGHT".
*/
public class AssociationContactLightEtablissementLightDao extends AbstractDao<AssociationContactLightEtablissementLight, Long> {

    public static final String TABLENAME = "ASSOCIATION_CONTACT_LIGHT_ETABLISSEMENT_LIGHT";

    /**
     * Properties of entity AssociationContactLightEtablissementLight.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ContactLightId = new Property(1, long.class, "contactLightId", false, "CONTACT_LIGHT_ID");
        public final static Property EtablissementLightId = new Property(2, long.class, "etablissementLightId", false, "ETABLISSEMENT_LIGHT_ID");
    }

    private DaoSession daoSession;


    public AssociationContactLightEtablissementLightDao(DaoConfig config) {
        super(config);
    }
    
    public AssociationContactLightEtablissementLightDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ASSOCIATION_CONTACT_LIGHT_ETABLISSEMENT_LIGHT\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CONTACT_LIGHT_ID\" INTEGER NOT NULL ," + // 1: contactLightId
                "\"ETABLISSEMENT_LIGHT_ID\" INTEGER NOT NULL );"); // 2: etablissementLightId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ASSOCIATION_CONTACT_LIGHT_ETABLISSEMENT_LIGHT\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AssociationContactLightEtablissementLight entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getContactLightId());
        stmt.bindLong(3, entity.getEtablissementLightId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AssociationContactLightEtablissementLight entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getContactLightId());
        stmt.bindLong(3, entity.getEtablissementLightId());
    }

    @Override
    protected final void attachEntity(AssociationContactLightEtablissementLight entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AssociationContactLightEtablissementLight readEntity(Cursor cursor, int offset) {
        AssociationContactLightEtablissementLight entity = new AssociationContactLightEtablissementLight( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // contactLightId
            cursor.getLong(offset + 2) // etablissementLightId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AssociationContactLightEtablissementLight entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setContactLightId(cursor.getLong(offset + 1));
        entity.setEtablissementLightId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AssociationContactLightEtablissementLight entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AssociationContactLightEtablissementLight entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AssociationContactLightEtablissementLight entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getContactLightDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getEtablissementLightDao().getAllColumns());
            builder.append(" FROM ASSOCIATION_CONTACT_LIGHT_ETABLISSEMENT_LIGHT T");
            builder.append(" LEFT JOIN CONTACT_LIGHT T0 ON T.\"CONTACT_LIGHT_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN ETABLISSEMENT_LIGHT T1 ON T.\"ETABLISSEMENT_LIGHT_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected AssociationContactLightEtablissementLight loadCurrentDeep(Cursor cursor, boolean lock) {
        AssociationContactLightEtablissementLight entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ContactLight contactLight = loadCurrentOther(daoSession.getContactLightDao(), cursor, offset);
         if(contactLight != null) {
            entity.setContactLight(contactLight);
        }
        offset += daoSession.getContactLightDao().getAllColumns().length;

        EtablissementLight etablissementLight = loadCurrentOther(daoSession.getEtablissementLightDao(), cursor, offset);
         if(etablissementLight != null) {
            entity.setEtablissementLight(etablissementLight);
        }

        return entity;    
    }

    public AssociationContactLightEtablissementLight loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<AssociationContactLightEtablissementLight> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<AssociationContactLightEtablissementLight> list = new ArrayList<AssociationContactLightEtablissementLight>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<AssociationContactLightEtablissementLight> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<AssociationContactLightEtablissementLight> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
