package com.pouillos.mypilulier.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.mypilulier.entities.Medicament;
import com.pouillos.mypilulier.entities.Prescription.FrequenceConverter;
import com.pouillos.mypilulier.enumeration.Frequence;

import com.pouillos.mypilulier.entities.Prescription;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "PRESCRIPTION".
*/
public class PrescriptionDao extends AbstractDao<Prescription, Long> {

    public static final String TABLENAME = "PRESCRIPTION";

    /**
     * Properties of entity Prescription.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MedicamentId = new Property(1, long.class, "medicamentId", false, "MEDICAMENT_ID");
        public final static Property Frequence = new Property(2, String.class, "frequence", false, "FREQUENCE");
        public final static Property DateFin = new Property(3, java.util.Date.class, "dateFin", false, "DATE_FIN");
    }

    private DaoSession daoSession;

    private final FrequenceConverter frequenceConverter = new FrequenceConverter();

    public PrescriptionDao(DaoConfig config) {
        super(config);
    }
    
    public PrescriptionDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"PRESCRIPTION\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"MEDICAMENT_ID\" INTEGER NOT NULL ," + // 1: medicamentId
                "\"FREQUENCE\" TEXT," + // 2: frequence
                "\"DATE_FIN\" INTEGER);"); // 3: dateFin
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"PRESCRIPTION\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Prescription entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getMedicamentId());
 
        Frequence frequence = entity.getFrequence();
        if (frequence != null) {
            stmt.bindString(3, frequenceConverter.convertToDatabaseValue(frequence));
        }
 
        java.util.Date dateFin = entity.getDateFin();
        if (dateFin != null) {
            stmt.bindLong(4, dateFin.getTime());
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Prescription entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getMedicamentId());
 
        Frequence frequence = entity.getFrequence();
        if (frequence != null) {
            stmt.bindString(3, frequenceConverter.convertToDatabaseValue(frequence));
        }
 
        java.util.Date dateFin = entity.getDateFin();
        if (dateFin != null) {
            stmt.bindLong(4, dateFin.getTime());
        }
    }

    @Override
    protected final void attachEntity(Prescription entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Prescription readEntity(Cursor cursor, int offset) {
        Prescription entity = new Prescription( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // medicamentId
            cursor.isNull(offset + 2) ? null : frequenceConverter.convertToEntityProperty(cursor.getString(offset + 2)), // frequence
            cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)) // dateFin
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Prescription entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMedicamentId(cursor.getLong(offset + 1));
        entity.setFrequence(cursor.isNull(offset + 2) ? null : frequenceConverter.convertToEntityProperty(cursor.getString(offset + 2)));
        entity.setDateFin(cursor.isNull(offset + 3) ? null : new java.util.Date(cursor.getLong(offset + 3)));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Prescription entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Prescription entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Prescription entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getMedicamentDao().getAllColumns());
            builder.append(" FROM PRESCRIPTION T");
            builder.append(" LEFT JOIN MEDICAMENT T0 ON T.\"MEDICAMENT_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Prescription loadCurrentDeep(Cursor cursor, boolean lock) {
        Prescription entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Medicament medicament = loadCurrentOther(daoSession.getMedicamentDao(), cursor, offset);
         if(medicament != null) {
            entity.setMedicament(medicament);
        }

        return entity;    
    }

    public Prescription loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Prescription> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Prescription> list = new ArrayList<Prescription>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Prescription> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Prescription> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
