package com.pouillos.mypilulier.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.mypilulier.entities.AlarmRdv;

import com.pouillos.mypilulier.entities.AssociationAlarmRdv;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ASSOCIATION_ALARM_RDV".
*/
public class AssociationAlarmRdvDao extends AbstractDao<AssociationAlarmRdv, Long> {

    public static final String TABLENAME = "ASSOCIATION_ALARM_RDV";

    /**
     * Properties of entity AssociationAlarmRdv.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property AlarmRdvId = new Property(1, long.class, "alarmRdvId", false, "ALARM_RDV_ID");
    }

    private DaoSession daoSession;


    public AssociationAlarmRdvDao(DaoConfig config) {
        super(config);
    }
    
    public AssociationAlarmRdvDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ASSOCIATION_ALARM_RDV\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"ALARM_RDV_ID\" INTEGER NOT NULL );"); // 1: alarmRdvId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ASSOCIATION_ALARM_RDV\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AssociationAlarmRdv entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getAlarmRdvId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AssociationAlarmRdv entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getAlarmRdvId());
    }

    @Override
    protected final void attachEntity(AssociationAlarmRdv entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AssociationAlarmRdv readEntity(Cursor cursor, int offset) {
        AssociationAlarmRdv entity = new AssociationAlarmRdv( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1) // alarmRdvId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AssociationAlarmRdv entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setAlarmRdvId(cursor.getLong(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AssociationAlarmRdv entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AssociationAlarmRdv entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AssociationAlarmRdv entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getAlarmRdvDao().getAllColumns());
            builder.append(" FROM ASSOCIATION_ALARM_RDV T");
            builder.append(" LEFT JOIN ALARM_RDV T0 ON T.\"ALARM_RDV_ID\"=T0.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected AssociationAlarmRdv loadCurrentDeep(Cursor cursor, boolean lock) {
        AssociationAlarmRdv entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        AlarmRdv alarmRdv = loadCurrentOther(daoSession.getAlarmRdvDao(), cursor, offset);
         if(alarmRdv != null) {
            entity.setAlarmRdv(alarmRdv);
        }

        return entity;    
    }

    public AssociationAlarmRdv loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<AssociationAlarmRdv> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<AssociationAlarmRdv> list = new ArrayList<AssociationAlarmRdv>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<AssociationAlarmRdv> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<AssociationAlarmRdv> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
