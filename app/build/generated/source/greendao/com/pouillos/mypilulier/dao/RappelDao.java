package com.pouillos.mypilulier.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.mypilulier.entities.Dose;
import com.pouillos.mypilulier.entities.Prescription;

import com.pouillos.mypilulier.entities.Rappel;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RAPPEL".
*/
public class RappelDao extends AbstractDao<Rappel, Long> {

    public static final String TABLENAME = "RAPPEL";

    /**
     * Properties of entity Rappel.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PrescriptionId = new Property(1, long.class, "prescriptionId", false, "PRESCRIPTION_ID");
        public final static Property QuantiteDose = new Property(2, Double.class, "quantiteDose", false, "QUANTITE_DOSE");
        public final static Property DoseId = new Property(3, long.class, "doseId", false, "DOSE_ID");
        public final static Property Heure = new Property(4, String.class, "heure", false, "HEURE");
    }

    private DaoSession daoSession;


    public RappelDao(DaoConfig config) {
        super(config);
    }
    
    public RappelDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RAPPEL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"PRESCRIPTION_ID\" INTEGER NOT NULL ," + // 1: prescriptionId
                "\"QUANTITE_DOSE\" REAL," + // 2: quantiteDose
                "\"DOSE_ID\" INTEGER NOT NULL ," + // 3: doseId
                "\"HEURE\" TEXT);"); // 4: heure
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RAPPEL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Rappel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPrescriptionId());
 
        Double quantiteDose = entity.getQuantiteDose();
        if (quantiteDose != null) {
            stmt.bindDouble(3, quantiteDose);
        }
        stmt.bindLong(4, entity.getDoseId());
 
        String heure = entity.getHeure();
        if (heure != null) {
            stmt.bindString(5, heure);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Rappel entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getPrescriptionId());
 
        Double quantiteDose = entity.getQuantiteDose();
        if (quantiteDose != null) {
            stmt.bindDouble(3, quantiteDose);
        }
        stmt.bindLong(4, entity.getDoseId());
 
        String heure = entity.getHeure();
        if (heure != null) {
            stmt.bindString(5, heure);
        }
    }

    @Override
    protected final void attachEntity(Rappel entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Rappel readEntity(Cursor cursor, int offset) {
        Rappel entity = new Rappel( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // prescriptionId
            cursor.isNull(offset + 2) ? null : cursor.getDouble(offset + 2), // quantiteDose
            cursor.getLong(offset + 3), // doseId
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4) // heure
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Rappel entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setPrescriptionId(cursor.getLong(offset + 1));
        entity.setQuantiteDose(cursor.isNull(offset + 2) ? null : cursor.getDouble(offset + 2));
        entity.setDoseId(cursor.getLong(offset + 3));
        entity.setHeure(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Rappel entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Rappel entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Rappel entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getPrescriptionDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getDoseDao().getAllColumns());
            builder.append(" FROM RAPPEL T");
            builder.append(" LEFT JOIN PRESCRIPTION T0 ON T.\"PRESCRIPTION_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN DOSE T1 ON T.\"DOSE_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected Rappel loadCurrentDeep(Cursor cursor, boolean lock) {
        Rappel entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        Prescription prescription = loadCurrentOther(daoSession.getPrescriptionDao(), cursor, offset);
         if(prescription != null) {
            entity.setPrescription(prescription);
        }
        offset += daoSession.getPrescriptionDao().getAllColumns().length;

        Dose dose = loadCurrentOther(daoSession.getDoseDao(), cursor, offset);
         if(dose != null) {
            entity.setDose(dose);
        }

        return entity;    
    }

    public Rappel loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<Rappel> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<Rappel> list = new ArrayList<Rappel>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<Rappel> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<Rappel> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
