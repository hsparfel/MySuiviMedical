package com.pouillos.mypilulier.dao;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.pouillos.mypilulier.entities.Dose;
import com.pouillos.mypilulier.entities.FormePharmaceutique;

import com.pouillos.mypilulier.entities.AssociationFormeDose;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ASSOCIATION_FORME_DOSE".
*/
public class AssociationFormeDoseDao extends AbstractDao<AssociationFormeDose, Long> {

    public static final String TABLENAME = "ASSOCIATION_FORME_DOSE";

    /**
     * Properties of entity AssociationFormeDose.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property FormePharmaceutiqueId = new Property(1, long.class, "formePharmaceutiqueId", false, "FORME_PHARMACEUTIQUE_ID");
        public final static Property DoseId = new Property(2, long.class, "doseId", false, "DOSE_ID");
    }

    private DaoSession daoSession;


    public AssociationFormeDoseDao(DaoConfig config) {
        super(config);
    }
    
    public AssociationFormeDoseDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ASSOCIATION_FORME_DOSE\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"FORME_PHARMACEUTIQUE_ID\" INTEGER NOT NULL ," + // 1: formePharmaceutiqueId
                "\"DOSE_ID\" INTEGER NOT NULL );"); // 2: doseId
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ASSOCIATION_FORME_DOSE\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AssociationFormeDose entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getFormePharmaceutiqueId());
        stmt.bindLong(3, entity.getDoseId());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AssociationFormeDose entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindLong(2, entity.getFormePharmaceutiqueId());
        stmt.bindLong(3, entity.getDoseId());
    }

    @Override
    protected final void attachEntity(AssociationFormeDose entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AssociationFormeDose readEntity(Cursor cursor, int offset) {
        AssociationFormeDose entity = new AssociationFormeDose( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getLong(offset + 1), // formePharmaceutiqueId
            cursor.getLong(offset + 2) // doseId
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AssociationFormeDose entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setFormePharmaceutiqueId(cursor.getLong(offset + 1));
        entity.setDoseId(cursor.getLong(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AssociationFormeDose entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AssociationFormeDose entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AssociationFormeDose entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getFormePharmaceutiqueDao().getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T1", daoSession.getDoseDao().getAllColumns());
            builder.append(" FROM ASSOCIATION_FORME_DOSE T");
            builder.append(" LEFT JOIN FORME_PHARMACEUTIQUE T0 ON T.\"FORME_PHARMACEUTIQUE_ID\"=T0.\"_id\"");
            builder.append(" LEFT JOIN DOSE T1 ON T.\"DOSE_ID\"=T1.\"_id\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected AssociationFormeDose loadCurrentDeep(Cursor cursor, boolean lock) {
        AssociationFormeDose entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        FormePharmaceutique formePharmaceutique = loadCurrentOther(daoSession.getFormePharmaceutiqueDao(), cursor, offset);
         if(formePharmaceutique != null) {
            entity.setFormePharmaceutique(formePharmaceutique);
        }
        offset += daoSession.getFormePharmaceutiqueDao().getAllColumns().length;

        Dose dose = loadCurrentOther(daoSession.getDoseDao(), cursor, offset);
         if(dose != null) {
            entity.setDose(dose);
        }

        return entity;    
    }

    public AssociationFormeDose loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<AssociationFormeDose> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<AssociationFormeDose> list = new ArrayList<AssociationFormeDose>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<AssociationFormeDose> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<AssociationFormeDose> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
